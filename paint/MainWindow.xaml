<Window x:Class="paint.MainWindow"
        KeyDown="Window_KeyDown"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="paint" Height="720" Width="1080">
    <Grid ClipToBounds="True" Background="#FFECECEC">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ToolBar Grid.Row="0">
            <ComboBox x:Name="toolComboBox" Width="100" SelectionChanged="ToolComboBox_SelectionChanged">
                <ComboBoxItem Content="Карандаш" IsSelected="True"/>
                <ComboBoxItem Content="Линия"/>
                <ComboBoxItem Content="Прямоугольник"/>
                <ComboBoxItem Content="Эллипс"/>
                <ComboBoxItem Content="Ластик"/>
            </ComboBox>
            <TextBlock Text="Цвет:" VerticalAlignment="Center" Margin="5,0"/>
            <ItemsControl x:Name="colorPalette" VerticalAlignment="Center">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Rectangle Width="20" Height="20" Margin="2" 
                                   Stroke="DarkGray" StrokeThickness="1"
                                   Cursor="Hand"
                                   MouseDown="ColorRectangle_MouseDown">
                            <Rectangle.Fill>
                                <SolidColorBrush Color="{Binding}"/>
                            </Rectangle.Fill>
                        </Rectangle>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Slider x:Name="brushSizeSlider" Width="100" Minimum="1" Maximum="50" Value="1" VerticalAlignment="Center" Margin="5, 0, 5, 0"/>
            <TextBlock Text="Размер: " VerticalAlignment="Center"/>
            <TextBlock x:Name="brushSizeText" Text="1" VerticalAlignment="Center" Width="25"/>
            <Border BorderBrush="Black" BorderThickness="1"  Margin="0, 0, 7, 0">
                <Button Content="Очистить" Click="ClearButton_Click"/>
            </Border>
            <Border BorderBrush="Black" BorderThickness="1" Margin="0, 0, 7, 0">
                <Button Content="Сохранить" Click="SaveButton_Click"/>
            </Border>
            <Border x:Name="rotateButton" BorderBrush="Black" BorderThickness="1">
                <Button Content="Поворот" Click="RotateCanvas_Click"/>
            </Border>

            <TextBlock Text="Фильтры: " VerticalAlignment="Center" Margin="30, 0, 0, 0"/>
            <Border x:Name="brightnessButton" BorderBrush="Black" BorderThickness="1" Margin="5, 0, 0, 0">
                <Button Content="Ярче" Click="ToggleBrightness_Click"/>
            </Border>
            <Border x:Name="invertButton" BorderBrush="Black" BorderThickness="1" Margin="5, 0, 0, 0">
                <Button Content="Инверсия" Click="ToggleInvert_Click"/>
            </Border>
            <Border x:Name="sepiaButton" BorderBrush="Black" BorderThickness="1" Margin="5, 0, 0, 0">
                <Button Content="Сепия" Click="ToggleSepia_Click"/>
            </Border>
            <Border x:Name="blurButton" BorderBrush="Black" BorderThickness="1" Margin="5, 0, 0, 0">
                <Button Content="Размытие" Click="ToggleBlur_Click"/>
            </Border>

            <Border x:Name="selectButton" BorderBrush="Black" BorderThickness="1" Margin="40, 0, 0, 0">
                <Button Content="Выделение" Click="SelectButton_Click"/>
            </Border>
            <Border x:Name="cropButton" BorderBrush="Black" BorderThickness="1" Margin="5, 0, 0, 0">
                <Button Content="Обрезать" Click="CropButton_Click"/>
            </Border>

        </ToolBar>

        <Border Grid.Row="1" 
        ClipToBounds="True" 
        Background="LightGray" 
        BorderThickness="2">
            <Canvas x:Name="drawingCanvas" Background="White"
                MouseMove="DrawingCanvas_MouseMove"
                MouseLeftButtonDown="DrawingCanvas_MouseLeftButtonDown"
                MouseLeftButtonUp="DrawingCanvas_MouseLeftButtonUp"/>
        </Border>

        <Border x:Name="selectionBorder" 
            BorderBrush="Blue" 
            BorderThickness="2"
            Visibility="Collapsed"
            Background="#20ADD8E6">
            <ContentControl x:Name="selectedContentControl"/>
        </Border>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock x:Name="statusText" Text="Готов к рисованию"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock x:Name="coordinatesText" Text="X: 0, Y: 0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
